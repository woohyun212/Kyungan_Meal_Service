2019-12-09
    예전에 만들었던 API형 급식봇을 버리고, 오픈빌더로 다시만들라니 착잡하다.
    JSON에 대해 깊히 다뤄본적없어서 어렵다. 오픈빌더에서 스킬 후에 응답 순으로 기능을 구현해야할 것 같다.

    일단 오늘 스킬테스트에서 JSON 값을 불러오는 익혔다.
    장고의 구조를 미리 먼저봤으면 더빨리 이해했었을 것이라는 느낌이 든다.

    500오류의 문제는 나의 리턴 값을 json dump 를 하지 않아서 생긴 오류였다.

    몇일간 삽질하면서 공부한 보람이있다.

2019-12-11
    모르겠다. 스킬데이터를 사용했을 땐, 분명히 잘 되었는데, 챗봇응답에서 500오류가 난다.
    이유가 뭘까. 알 수 없다..JnL

2019-12-12
    잠이 안와서 밤새고 6시간 동안 급식 식단 파서와 봇 응답 구현을 완료했다.

    엔티티에서 date 값을 불러오면 2019-12-12이런 식의 문자열이라 코드한줄을 늘려주신다. 주의해야할 것 같다.
    이유는 모르겟지만 엔티티를 사용하여 답변을 할 때 엔티티 내부의 값들이 str 로 이뤄져있어
    다시 dict 형으로 변경하여 사용해야하는 번거로움이있다.

    JSON 내부 메소드에 dumps 와 dump, loads, load 가 있던데 아직 차이는 잘 모르겠다.
    그래도 dump는 json의 형태로, load는 dict의 형태로 바꿔준다는 것은 알겠다.

    예전 톡방에 아직도 버튼들이 남아있다. 지우는 법은 없을까?

2019-12-31
    다시 개발 시작해보자 안건든지 오래됐다.
    이거 다만들고 고대로 상록고도 만들면 되지않을까?
